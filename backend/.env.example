# ClassTwin AI — 환경변수 템플릿
# 이 파일을 복사해서 `.env`로 이름을 바꾸고 실제 키를 입력하세요.
#   Copy-Item .env.example .env

# OpenRouter API 키 (https://openrouter.ai 에서 발급)
# - 추가고려사항 자연어 파싱, AI 문서 생성 등 LLM 기능에 사용됩니다.
# - 비워두면 규칙 기반 fallback으로 동작합니다 (기능 제한 있음).
OPENROUTER_API_KEY=